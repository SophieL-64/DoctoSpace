gridGallery({
    // gallery selector
    selector: "#mSelector",
    // or "horizontal"
    layout: "square",
    // space between images
    gapLength: 4,
    // row height
    rowHeight: 180,
    // column width
    columnWidth: 200
});
