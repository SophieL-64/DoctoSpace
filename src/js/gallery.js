gridGallery({
    // gallery selector
    selector: "#mSelector",
    // enable dark mode
    darkMode: true,
    // or "horizontal"
    layout: "square",
    // space between images
    gapLength: 4,
    // row height
    rowHeight: 180,
    // column width
    columnWidth: 200
});